# Workflow: /ingest-json

When the user types `/ingest-json`, the agent must immediately execute the following skill:

- **Skill File**: [ingest_json.md](file:///C:/Users/antho/.gemini/antigravity/scratch/market-intel-web/skills/ingest_json.md)
- **Primary Action**: Run the bulk ingestion script for structured JSON reports.

## Execution Steps
1.  Run the command: `npm run sync-json -- "C:\Users\antho\Desktop\Intel Reports"`
2.  Report the results to the user.
